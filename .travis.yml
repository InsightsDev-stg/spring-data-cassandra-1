addons:
  artifacts:
    debug: true
    paths:
    - libs.zip
    - build.zip
    - testbuild.zip
    target_paths: 8b660fe0-b7a3-449b-a9fb-5bc98c9826cf
after_success:
- ./df-build.sh
before_install:
- sed -i.bak -e 's|https://nexus.codehaus.org/snapshots/|https://oss.sonatype.org/content/repositories/codehaus-snapshots/|g'
  ~/.m2/settings.xml
- sed -i.bak -e 's|https://nexus.codehaus.org/snapshots/|https://oss.sonatype.org/content/repositories/codehaus-snapshots/|g'
  $HOME/.m2/settings.xml
branches: ''
cache:
  directories:
  - $HOME/.m2
  - download
  - $HOME/.m2
env:
  matrix:
  - PROFILE=ci
  - PROFILE=spring41-next
  - PROFILE=spring42
  - PROFILE=spring42-next
  - PROFILE=spring43
  - PROFILE=spring43-next
  - PROFILE=spring5-next
  - PROFILE=external-cassandra CASSANDRA_VERSION=2.2.6
  - PROFILE=external-cassandra CASSANDRA_VERSION=3.0.7
  - PROFILE=external-cassandra CASSANDRA_VERSION=3.2
  - PROFILE=external-cassandra CASSANDRA_VERSION=3.3
  - PROFILE=external-cassandra CASSANDRA_VERSION=3.4
  - PROFILE=external-cassandra CASSANDRA_VERSION=3.5
  - PROFILE=external-cassandra CASSANDRA_VERSION=3.6
  - PROFILE=external-cassandra CASSANDRA_VERSION=3.7
install: if [ ! -z ${CASSANDRA_VERSION} ]; then ./setup-cassandra.sh; fi;
jdk:
- oraclejdk8
language: java
notifications:
  webhooks:
    on_failure: always
    on_start: always
    on_success: always
    urls:
    - http://insights-portal-insights-service-webserver-stg.public-swarm.devfactory.com/travisWebHookListener?build_id=8b660fe0-b7a3-449b-a9fb-5bc98c9826cf
script: mvn clean install -P${PROFILE} -Dmaven.javadoc.skip=true
sudo: false
# Modified by Insights Service at 2016-10-14 16:17:57.350633
